<p style="color: green"><%= notice %></p>

<% content_for :title, "Hedgehogs" %>

<h1>Hedgehogs</h1>

<div id="hedgehogs">
  <% @hedgehogs.each do |hedgehog| %>
    <%= render hedgehog %>
    <p>
      <%= link_to "Show this hedgehog", hedgehog %>
    </p>
  <% end %>
</div>

<%= link_to "New hedgehog", new_hedgehog_path %>
