<div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4 notification-item" data-notification-id="<%= Time.current.to_i %>">
  <div class="flex items-center justify-between">
    <div class="flex items-center">
      <% if job_type == "EmailJob" %>
        <span class="text-2xl mr-2">📧</span>
      <% else %>
        <span class="text-2xl mr-2">🎉</span>
      <% end %>
      <div>
        <p class="font-bold">
          <%= job_type %> が完了しました！
        </p>
        <p class="text-sm">
          <% if job_type == "EmailJob" %>
            メール送信先: <%= name %> | 件名: <%= message %>
          <% else %>
            名前: <%= name %> | メッセージ: <%= message %>
          <% end %>
        </p>
        <p class="text-xs text-green-600">
          完了時刻: <%= completed_at %>
        </p>
      </div>
    </div>
    <button onclick="this.parentElement.parentElement.remove()" class="text-green-700 hover:text-green-900 font-bold text-xl">
      ×
    </button>
  </div>
</div>
 
